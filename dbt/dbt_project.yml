name: 'lab5_bi'
version: '1.0.0'
config-version: 2

profile: 'lab5_bi'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  lab5_bi:
    +schema: Staging
    +materialized: table
    staging:
      +materialized: view
    marts:
      +materialized: table
